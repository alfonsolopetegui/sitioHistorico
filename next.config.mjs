/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['firebasestorage.googleapis.com'], // Agrega el dominio de Firebase
    },
  };
  
  export default nextConfig;
  